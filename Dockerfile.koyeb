# --- EASYFIT BOT Dockerfile per Koyeb ---
FROM python:3.11-slim

# Imposta directory di lavoro
WORKDIR /app

# Copia i file
COPY . /app

# Installa le dipendenze
RUN pip install --no-cache-dir -r requirements.txt

# Espone la porta usata dal keep-alive Flask
EXPOSE 8080

# Avvio del bot
CMD ["python", "bot.py"]
